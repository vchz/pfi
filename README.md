This package implements probability flow inference. You can easily install it with the following commands:
`git clone git@github.com:vchz/pfi.git
cd pfi
pip install -e .`
You can find the documentation here: https://vchz.github.io/pfi/. We will soon add more data analysis on single-cell RNA-seq data.
